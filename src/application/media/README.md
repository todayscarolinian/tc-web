# application/media

Not yet implemented. Future use-cases:

- `listMediaAssets(repo, filter?)`
- `getMediaAssetById(repo, id)`

See `domain/media/README.md` — note the `iconKey` redesign needed before
`components/staff/media-view.tsx` can take this data as props.
